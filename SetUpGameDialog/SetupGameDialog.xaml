<Window x:Class="Othello.View.SetupGameDialog" 
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="Othello.View:SetUpGameDialog"
        mc:Ignorable="d"
        FontFamily="Courier New"
        FontSize="15"
        Title="New Game" Height="280" Width="400"
        WindowStartupLocation = "CenterScreen" ResizeMode = "NoResize" ShowInTaskbar = "False">
    <Window.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="LawnGreen" Offset="0"/>
            <GradientStop Color="DarkGreen" Offset="1"/>
        </LinearGradientBrush>
    </Window.Background>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                     From="0" To="1" Duration="0:0:1"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>
    
    <StackPanel Margin="10">
        <TextBlock Text="Player 1 Name:" FontWeight="SemiBold"/>
        <TextBox x:Name="Player1NameBox"/>
        <TextBlock Text="Player 1 Type:" FontWeight="SemiBold"/>
        <ComboBox x:Name="Player1TypeBox" SelectedIndex="0">
            <ComboBoxItem Content="Human"/>
            <ComboBoxItem Content="Computer"/>
        </ComboBox>

        <TextBlock Text="Player 2 Name:" Margin="0,10,0,0" FontWeight="SemiBold"/>
        <TextBox x:Name="Player2NameBox"/>

        <TextBlock Text="Player 2 Type:" FontWeight="SemiBold"/>
        <ComboBox x:Name="Player2TypeBox" SelectedIndex="1">
            <ComboBoxItem Content="Human"/>
            <ComboBoxItem Content="Computer"/>
        </ComboBox>

        <Button Content="Start Game" Margin="0,20,0,0" Click="NewGameClick" FontWeight="SemiBold" Background="LimeGreen"/>
    </StackPanel>
</Window>
