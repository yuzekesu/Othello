<UserControl x:Class="Othello.View.TempGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Othello.View"
             xmlns:viewmodel="clr-namespace:Othello.ViewModel;assembly=MainViewModel" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <!--
    <UniformGrid Name="UniformGrid">
    </UniformGrid>
    >-->
    <ItemsControl ItemsSource="{Binding Square}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="8" Columns="8"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                <Button Command="{Binding SquareClickCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            CommandParameter="{Binding}" Background="{Binding Path=Color, Converter=ColorToBrush}">
                </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate> 

</UserControl>
