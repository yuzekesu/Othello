<UserControl x:Class="Test.View.GameGrid"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Test.View"
        xmlns:converter="clr-namespace:Test.ViewModel"
        mc:Ignorable="d"
        d:DesignHeight="600" d:DesignWidth="600"
        DataContext="{StaticResource MainViewModel}">

    <UserControl.Resources>
        <converter:ColorToBrush x:Key="ColorToBrush" />
    </UserControl.Resources>

    <DockPanel>
        <ItemsControl ItemsSource="{Binding ObservSquares}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="8" Columns="8"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Command="{Binding SquareClickCommand, Source={StaticResource MainViewModel}}"
                            CommandParameter="{Binding}" Background="Green">
                        <Ellipse Fill="{Binding Path=Color, Converter={StaticResource ColorToBrush}}" Width="45" Height="45" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </DockPanel>
</UserControl>
